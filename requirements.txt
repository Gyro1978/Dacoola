# requirements.txt

# Core & Web
requests
python-dotenv
beautifulsoup4
trafilatura
feedparser # For RSS feed processing in SiteMonitorAgent
htmldate # For precise date extraction
requests-html # Optional: for JavaScript rendering in SiteMonitorAgent (if enabled)
lxml # Often a faster parser for BeautifulSoup and used by other libs

# Parsing & Content
Markdown # For Markdown to HTML conversion
Jinja2   # For HTML templating

# Image Processing & Analysis
Pillow             
imagehash          

# AI & Embeddings (Local Focus & Hugging Face)
sentence-transformers 
torch                 
transformers          
einops                
# accelerate            # Optional: For optimizing HF model loading & inference
# bitsandbytes          # Optional: For 8-bit/4-bit quantization of HF models

# Social Media
tweepy>=4.0.0         
atproto               
praw                  

# Utilities
pyperclip             # For prompt-maker.py (optional)
textstat              # For readability scores

# Google Search Results (SerpApi Client) - No longer primary for web_research_agent
# but kept if vision_media_agent or other parts use it.
google-search-results 