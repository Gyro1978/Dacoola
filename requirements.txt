# requirements.txt

# Core & Web
requests
python-dotenv
beautifulsoup4
trafilatura
duckduckgo_search # For Web Research Agent

# Parsing & Content
feedparser
Markdown
Jinja2

# Image Processing
Pillow

# AI & Embeddings (Local Focus)
sentence-transformers # Let this pull its dependencies (torch, transformers, etc.)
# numpy will be pulled as a dependency by sentence-transformers/torch

# Social Media
tweepy
atproto
praw

# Utilities
pyperclip
textstat

# If you are certain you are no longer using DeepSeek or SerpApi directly:
# DEEPSEEK_API_KEY (Handled via env, no library needed for direct HTTP)
# google-search-results # SerpApi client - REMOVE if not used

# If not using Google Ads API for keywords:
# google-ads # REMOVE if not used